export const metadata = {
  title: "SEB Gateway",
  description: "Secure gateway for LMSGuard SEB access",
};

export default function SebLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
